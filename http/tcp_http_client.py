# # import socket
# # def main():
# #     host ='localhost'
# #     port = 8080
# #     hostname = (host,port)
# #     with socket.socket(socket.AF_INET,socket.SOCK_STREAM) as s:
# #         s.connect(hostname)
# #         print('socket status after connection%s '% (s))
# #         print()
